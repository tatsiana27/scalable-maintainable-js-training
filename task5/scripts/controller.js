define("AppController", [
	"questionsCollection",
	"questionsView"]
	function (QuestionsCollection, QuestionsView) {
		console.log('hello');
		var QuestionsView = Backbone.View.extend({
			url:
			initlize:
			..
		});
		var myCol new Collect

		myCol.fetch().complete(function () {
			var sds new View {
				collection: myCol (this)
			}

			view.on('dasdad', function () {

			})

		});

		return QuestionsView;

	});


// <% _.each(items, function(item) { %>
		// 	<label class="answer"><input type="radio" data-answer="<%- item.id %>"><%- item.value%></label><br>
		// <%}); %>
<span><%= result%></span>